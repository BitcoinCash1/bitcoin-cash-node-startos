import { VersionInfo } from '@start9labs/start-sdk'

export const v_29_0_0_3 = VersionInfo.of({
  version: '29.0.0:3',
  releaseNotes: 'Adds instructions.',
  migrations: {
    up: async ({ effects }) => {},
    down: async ({ effects }) => {},
  },
})
